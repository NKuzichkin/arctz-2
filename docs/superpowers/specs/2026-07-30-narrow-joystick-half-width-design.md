# Джойстики на узких экранах: по половине ширины каждый — дизайн

Дата: 2026-07-30

## Проблема

После адаптивной раскладки узкого экрана (`docs/superpowers/specs/2026-07-30-responsive-narrow-screen-layout-design.md`) джойстики в узком режиме (`ContentGrid.narrow`) имеют фиксированный `Radius=55` и центрируются парой вместе (гибкие распорки-колонки `*,Auto,Auto,*`) — то есть маленькие и сгруппированы в середине экрана. Нужно, чтобы каждый джойстик занимал свою половину ширины экрана и был заметно крупнее, масштабируясь под доступное место, а не под одно фиксированное число.

## Раскладка колонок

`ContentGrid.narrow` `ColumnDefinitions` меняется с `"*,Auto,Auto,*"` на `"*,*"` — две равные звёздочные колонки (50/50). `LeftJoystick` → `Grid.Column=0`, `RightJoystick` → `Grid.Column=1`, оба `HorizontalAlignment="Center"` — центр в своей половине. Асимметричные `Margin="0,0,20,0"`/`"20,0,0,0"` убираются — при отдельном центрировании в своей колонке они не нужны, отступ от края/соседа обеспечивается запасом в формуле радиуса (см. ниже). `ProgramPanel.narrow` `Grid.ColumnSpan` меняется с `4` на `2` (панель по-прежнему растягивается на всю ширину, но теперь всего 2 колонки).

## Динамический радиус

`Radius` перестаёт быть статичным `Style Setter` (было `55`) в узком режиме — вычисляется в code-behind при каждом изменении ширины, пока активен узкий режим:

```
overhead = 54   // Border(reveal-3).Margin(0,12,12,12→12) + BorderThickness(2) + ContentGrid.Margin(20+20=40)
contentGridWidth = НоваяШиринаMainView - overhead
columnWidth = contentGridWidth / 2
radius = Max(50, columnWidth / 2 - 12)
```

`50` — нижний порог радиуса на случай экстремально узких экранов (защита от вырожденного размера при ширине окна около 320px). `12` — запас от края колонки/соседнего джойстика, чтобы диаметр не касался соседа или границы. При переходе на широкий экран (`isNarrow == false`) `Radius` очищается через `ClearValue(VirtualJoystick.RadiusProperty)`, чтобы снова действовал существующий статичный `Style Setter="80"`.

Текущий guard в `OnSizeChanged` — `if (_isNarrow == isNarrow) return;` — пропускает пересчёт при ресайзах **внутри** одного режима (например, поворот телефона 375→414px не меняет `isNarrow`, но должен пересчитать радиус). Обработчик реструктурируется: переключение класса `narrow`/`ColumnDefinitions` по-прежнему происходит только при смене режима, а расчёт и применение `Radius` — на каждом срабатывании `SizeChanged`, пока `isNarrow == true`.

## Затронутые файлы

- `ArctZ/Views/MainView.axaml` — `ColumnDefinitions`/`ColumnSpan` narrow-стилей для джойстиков и `ProgramPanel`, убрать `Radius="55"` и `Margin` из narrow-стилей джойстиков.
- `ArctZ/Views/MainView.axaml.cs` — расширить `OnSizeChanged`: вычисление и применение динамического `Radius`, реструктуризация guard'а.

## Не в скоупе

- Изменение раскладки/размера панели программы — остаётся сверху на всю ширину, без изменений (см. `2026-07-30-responsive-narrow-screen-layout-design.md`).
- Изменение широкого режима (`isNarrow == false`) — `Radius=80` через статичный `Style Setter` остаётся как есть.
- Тесты — как и для родительской раскладки, в решении нет View-тестов; проверка через `dotnet build` и визуально через `ArctZ.Browser` + Playwright (ресайз вьюпорта на нескольких узких ширинах: 320, 360, 414px).
